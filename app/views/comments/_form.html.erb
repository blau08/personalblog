<%= render('layouts/errors', :object => @comment) %>

<%= form_for [@post, @comment] do |f| %>
  <%= f.label :user %>
  <%= f.text_field :user %>
  <%= f.label :comment %>
  <%= f.text_field :comment %>
  <p><%= f.submit %></p>
<% end %>
